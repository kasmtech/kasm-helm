{{/*
Deploy Kasm API Service .Values.components.api service definition
*/}}
{{- $constants := (include "kasm.constants" . | fromYaml) -}}
{{- range $idx, $zone := (.Values.kasmZones | default (list (dict "name" "default"))) }}
---
apiVersion: v1
kind: Service
metadata:
  {{- include "kasm.metadata" (dict "context" $ "component" "api" "resource" "service" "zone" $zone.name "include" "all") | indent 2 }}
spec:
  selector:
    {{- include "kasm.metadata" (dict "context" $ "component" "api" "resource" "service" "zone" $zone.name "include" "match") | indent 6 }}
  ports:
    - name: {{ printf "%s-svc-%s" $constants.api.svc ($constants.api.port | toString) }}
      protocol: TCP
      port: {{ $constants.api.port | toString }}
{{- end }}