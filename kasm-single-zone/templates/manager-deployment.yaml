{{/*
Kasm Manager server Deployment definition
*/}}
{{- $managerComponent := "manager" -}}
{{- $managerName := (printf "%s-%s" .Release.Name $managerComponent) -}}
{{- $managerImage := (printf "%s:%s" .Values.components.manager.image.repository .Values.components.manager.image.tag) -}}
{{- $managerReplicas := include "replicas.preset" (dict "node" $managerComponent "size" .Values.deploymentSize) -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $managerName }}-deployment
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $managerName }}
    app.kubernetes.io/component: {{ $managerComponent }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or .Values.components.manager.labels .Values.extraLabels.deployment .Value.labels }}
    {{- with .Values.components.manager.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.extraLabels.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- if or .Values.components.manager.annotations .Values.annotations.deployment }}
  annotations:
    {{- with .Values.annotations.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.components.manager.annotations }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
spec:
  replicas: {{ $managerReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 $managerReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $managerName }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $managerName }}
        app.kubernetes.io/component: {{ $managerComponent }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
      {{- if or .Values.components.manager.labels .Values.extraLabels.pod .Value.labels }}
        {{- with .Values.components.manager.labels }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.extraLabels.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
      {{- end }}
      {{- if or .Values.components.manager.annotations .Values.annotations.pod }}
      annotations:
        {{- with .Values.annotations.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.components.manager.annotations }}{{- toYaml . | indent 8 }}{{- end }}
      {{- end }}
    spec:
      restartPolicy: {{ .Values.restartPolicy }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.applySecurity }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
        {{- include "kasm.initContainer" (dict "serviceName" $apiName "servicePort" "8080" "path" "/api/__healthcheck" "schema" "http") | nindent 8 }}
        {{- if .Values.applySecurity }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
      containers:
        - name: {{ $managerName }}-container
          image: {{ $managerImage }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: LOG_LEVEL
              value: INFO
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: db-password
          ports:
            - name: {{ $managerName }}-pt
              containerPort: 8181
              protocol: TCP
          {{- if .Values.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" $managerImage) ) | indent 12 }}
          {{- end }}
          {{- if .Values.addHealthChecks }}
          readinessProbe:
            {{- include "health.http" (dict "path" "/__healthcheck" "portName" (printf "%s-pt" $managerImage) ) | indent 12 }}
          {{- end }}
          {{- if .Values.components.manager.resources }}
          resources: {{- toYaml .Values.components.manager.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" $managerComponent "size" .Values.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
          volumeMounts:
            - name: {{ $managerName }}-tmp
              mountPath: /tmp
      volumes:
        - name: {{ $managerName }}-tmp
          emptyDir:
            sizeLimit: 1Gi