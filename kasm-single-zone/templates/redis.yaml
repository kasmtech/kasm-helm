apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.kasmApp.name | default "kasm" }}
    kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-redis
  name: {{ .Values.kasmApp.name | default "kasm" }}-redis
  namespace: {{ .Values.namespace | default .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-redis
  strategy: {}
  template:
    metadata:
      labels:
        app: {{ .Values.kasmApp.name | default "kasm" }}
        kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-redis
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: kasm-redis
          image: redis:5-alpine
          imagePullPolicy: Always
          resources: {}
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: kasm-secrets
                  key: redis-password
          command:
            - "redis-server"
          args: 
            - "--requirepass"
            - "$(REDIS_PASSWORD)"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.kasmApp.name | default "kasm" }}
  name: kasm-redis
  namespace: {{ .Values.namespace | default .Release.Namespace }}
spec:
  selector:
    kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-redis
  ports:
  - name: kasm-redis
    protocol: TCP
    port: 6379
