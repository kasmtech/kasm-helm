apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.kasmApp.name | default "kasm" }}-db
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-db
{{- include "kasm.defaultLabels" . | indent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.kasmApp.name | default "kasm" }}
  template:
    metadata:
      labels:
        kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-db
{{- include "kasm.defaultLabels" . | indent 8 }}
    spec:
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}
{{- end }}
      containers:
          - name: kasm-db
            env:
              - name: POSTGRES_DB
                value: kasm
              - name: POSTGRES_PASSWORD
                valueFrom:
                  secretKeyRef:
                    name: {{ .Values.kasmApp.name | default "kasm" }}-secrets
                    key: db-password
              - name: POSTGRES_USER
                value: kasmapp
            image: kasmweb/postgres-private:feature_KASM-5313_webui_code_move
            resources: {}
            volumeMounts:
              - name: kasm-db
                mountPath: /var/lib/postgresql/data
                subPath: postgres
              - name: kasm-db-sql-config
                mountPath: /docker-entrypoint-initdb.d/data.sql
                subPath: data.sql
              - name: kasm-db-config
                mountPath: /var/lib/postgresql/conf
      volumes:
        - name: kasm-db
          persistentVolumeClaim:
            claimName: kasm-db-pvc
        - name: kasm-db-sql-config
          configMap:
            name: {{ .Values.kasmApp.name | default "kasm" }}-db-configmap
        - name: kasm-db-config
          configMap:
            name: {{ .Values.kasmApp.name | default "kasm" }}-db-configmap
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kasm-db-pvc
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
{{- include "kasm.defaultLabels" . | indent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
---
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
{{- include "kasm.defaultLabels" . | indent 4 }}
spec:
  selector:
    kasm_app: {{ .Values.kasmApp.name | default "kasm" }}-db
  ports:
  - name: db
    protocol: TCP
    port: 5432