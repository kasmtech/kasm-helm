{{/*
Kasm Proxy server Deployment definition
*/}}
{{- $values := (include "kasm.constants" . | fromYaml) -}}
{{- $proxyReplicas := include "replicas.preset" (dict "node" $values.proxy.component "size" .Values.deploymentSize) -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $values.proxy.name }}-deployment
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $values.proxy.name }}
    app.kubernetes.io/component: {{ $values.proxy.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or .Values.components.proxy.labels .Values.extraLabels.deployment .Values.labels }}
    {{- with .Values.components.proxy.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.extraLabels.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- if or .Values.components.proxy.annotations .Values.annotations.deployment }}
  annotations:
    {{- with .Values.annotations.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.components.proxy.annotations }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
spec:
  replicas: {{ $proxyReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 $proxyReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $values.proxy.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $values.proxy.name }}
        app.kubernetes.io/component: {{ $values.proxy.component }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
      {{- if or .Values.components.proxy.labels .Values.extraLabels.pod .Values.labels }}
        {{- with .Values.components.proxy.labels }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.extraLabels.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
      {{- end }}
      {{- if or .Values.components.proxy.annotations .Values.annotations.pod }}
      annotations:
        {{- with .Values.annotations.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.components.proxy.annotations }}{{- toYaml . | indent 8 }}{{- end }}
      {{- end }}
    spec:
      restartPolicy: {{ .Values.restartPolicy }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.applyHardening }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
        {{- if .Values.applyHardening }}
        - name: {{ $values.proxy.name }}-data-perms
          image: {{ $values.api.image }}
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -ec
          args:
            - |
              echo "Setting permissions and ownership for /var/cache/nginx..." && chown -R 1000:1000 /var/cache/nginx && chmod -R 755 /var/cache/nginx
              echo "Setting permissions and ownership for /var/log/external/nginx..." && chown -R 1000:1000 /var/log/external/nginx && chmod -R 755 /var/log/external/nginx
              echo "Setting permissions and ownership for /tmp..." && chmod -R 777 /tmp
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          volumeMounts:
            - name: {{ $values.proxy.name }}-data
              mountPath: /var/cache/nginx
            - name: {{ $values.proxy.name }}-data
              mountPath: /var/log/external/nginx
            - name: {{ $values.proxy.name }}-data
              mountPath: /run
        {{- end }}
        {{- include "kasm.initContainer" (dict "serviceName" $values.api.svc "servicePort" $values.api.port "path" "/api/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
        {{- if .Values.applyHardening }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
        {{- include "kasm.initContainer" (dict "serviceName" $values.manager.svc "servicePort" $values.manager.port "path" "/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
        {{- if .Values.applyHardening }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
        {{- if .Values.components.share.enabled }}
          {{- include "kasm.initContainer" (dict "serviceName" $values.share.svc "servicePort" $values.share.port "path" "/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
        {{- end }}
        {{- if .Values.components.guac.enabled }}
          {{- include "kasm.initContainer" (dict "serviceName" $values.guac.svc "servicePort" $values.guac.port "path" "/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
        {{- end }}
        {{- if .Values.components.rdpGateway.enabled }}
          {{- include "kasm.initContainer" (dict "serviceName" $values.rdpGateway.svc "servicePort" $values.rdpGateway.port "path" "/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
        {{- end }}
      containers:
        - name: {{ $values.proxy.name }}-container
          image: {{ $values.proxy.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          {{- if .Values.components.proxy.resources }}
          resources: {{- toYaml .Values.components.proxy.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" $values.proxy.component "size" .Values.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
          volumeMounts:
            - name: {{ $values.proxy.name }}-cert
              mountPath: /etc/ssl/certs/kasm_nginx.crt
              subPath: tls.crt
            - name: {{ $values.proxy.name }}-cert
              mountPath: /etc/ssl/private/kasm_nginx.key
              subPath: tls.key
            - name: {{ $values.proxy.name }}-settings-config
              mountPath: /etc/nginx/conf.d
            - name: {{ $values.proxy.name }}-services-config
              mountPath: /etc/nginx/conf.d/services.d
            - name: {{ $values.proxy.name }}-base-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            {{- if .Values.applyHardening }}
            - name: {{ $values.proxy.name }}-data
              mountPath: /var/cache/nginx
            - name: {{ $values.proxy.name }}-data
              mountPath: /var/log/external/nginx
            - name: {{ $values.proxy.name }}-data
              mountPath: /run
            {{- end }}
          ports:
            - name: {{ $values.proxy.name }}-pt
              containerPort: {{ $values.proxy.http }}
              protocol: TCP
          {{- if .Values.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" $values.proxy.name) ) | indent 12 }}
          {{- end }}
          {{- if .Values.addHealthChecks }}
          readinessProbe:
            {{- include "health.command" (dict "command" "nginx -t") | indent 12 }}
          {{- end }}
      volumes:
        - name: {{ $values.proxy.name }}-cert
          secret:
            secretName: {{ .Values.certificate.secretName }}-certs
        - name: {{ $values.proxy.name }}-base-config
          configMap:
            name: {{ $values.proxy.name }}-base-configmap
        - name: {{ $values.proxy.name }}-settings-config
          configMap:
            name: {{ $values.proxy.name }}-settings-configmap
        - name: {{ $values.proxy.name }}-services-config
          configMap:
            name: {{ $values.proxy.name }}-services-configmap
        {{- if .Values.applyHardening }}
        - name: {{ $values.proxy.name }}-data
          emptyDir:
            sizeLimit: 1Gi
        {{- end }}
