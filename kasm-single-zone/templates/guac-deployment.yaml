{{/*
Deploy the Guacamole Web RDP if .Values.components.guac.enabled set to true
*/}}
{{- if .Values.components.guac.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.components.guac.name }}-deployment
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.components.guac.name }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
spec:
  replicas: {{ .Values.components.guac.replicas | default (include "replicas.preset" (dict "node" "guac" "size" .Values.deploymentSize)) }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 ( default (include "replicas.preset" (dict "node" "guac" "size" .Values.deploymentSize))) }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.components.guac.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.components.guac.name }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
    spec:
      restartPolicy: {{ .Values.global.image.restartPolicy }}
      {{- if .Values.global.image.pullSecrets }}
        {{- include "image.pullSecrets" . | indent 6 }}
      {{- end }}
      {{- if .Values.applyHardening }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
      {{- if .Values.applyHardening }}
        - name: guac-tmp-perms
          image: busybox
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -c
            - |
              echo "Setting permissions and ownership for /tmp..." && chown -R 1000:1000 /tmp && chmod -R 777 /tmp
              echo "Setting permissions and ownership for /opt/kasm/current..." && chown -R 1000:1000 /opt/kasm/current && chmod -R 0770 /opt/kasm/current
              echo "Done"
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
        {{- end }}
        {{- include "kasm.initContainer" (dict "serviceName" "kasm-api" "servicePort" "8080" "path" "/api/__healthcheck" "schema" "http") | nindent 8 }}
        {{- if .Values.applyHardening }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
      containers:
        - name: {{ .Values.components.guac.name }}-container
          image: "{{ .Values.components.guac.image }}:{{ .Values.components.guac.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          env:
            - name: CLUSTER_SIZE
              value: "4"
            - name: REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kasmApp.name | default "kasm" }}-secrets
                  key: service-token
            - name: ZONE
              value: default
            - name: KUBERNETES_SERVICE_HOST
              value: "true"
            - name: SERVER_ADDRESS
              value: "kasm-proxy"
            - name: SERVER_PORT
              value: "8080"
          ports:
            - name: {{ .Values.components.guac.name }}-pt
              containerPort: 3000
              protocol: TCP
          {{- if .Values.applyHardening }}
          volumeMounts:
            - name: kasm-guac-data
              mountPath: /tmp
            - name: kasm-guac-readonly
              mountPath: /opt/kasm/current/log
              readOnly: true
            - name: kasm-guac-data
              mountPath: /opt/kasm/current/tmp/guac
          {{- end }}
          {{- if .Values.components.guac.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" .Values.components.guac.name) ) | indent 12 }}
          {{- end }}
          {{- if .Values.components.guac.addHealthChecks }}
          readinessProbe:
            {{- include "health.http" (dict "path" "/__healthcheck" "portName" (printf "%s-pt" .Values.components.guac.name) ) | indent 12 }}
          {{- end }}
          {{- if .Values.components.guac.resources }}
          resources: {{- toYaml .Values.components.guac.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" "guac" "size" .Values.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
      {{- if .Values.applyHardening }}
      volumes:
        - name: kasm-guac-data
          emptyDir:
            sizeLimit: 1Gi
        - name: kasm-guac-readonly
          emptyDir:
            sizeLimit: 150Mi
      {{- end }}
{{- end }}
