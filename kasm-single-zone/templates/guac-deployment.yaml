{{/*
Deploy the Guacamole Web RDP if .Values.components.guac.enabled set to true
*/}}
{{- $values := (include "kasm.constants" . | fromYaml) -}}
{{- $guacReplicas := include "replicas.preset" (dict "node" ($values.guac.component) "size" .Values.deploymentSize) -}}
{{- if eq .Values.components.guac.enabled true }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $values.guac.name }}-deployment
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $values.guac.name }}
    app.kubernetes.io/component: {{ $values.guac.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or .Values.components.guac.labels .Values.extraLabels.deployment .Values.labels }}
    {{- with .Values.components.guac.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.extraLabels.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- if or .Values.components.guac.annotations .Values.annotations.deployment }}
  annotations:
    {{- with .Values.annotations.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.components.guac.annotations }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
spec:
  replicas: {{ $guacReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 $guacReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $values.guac.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $values.guac.name }}
        app.kubernetes.io/component: {{ $values.guac.component }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
      {{- if or .Values.components.guac.labels .Values.extraLabels.pod .Values.labels }}
        {{- with .Values.components.guac.labels }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.extraLabels.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
      {{- end }}
      {{- if or .Values.components.guac.annotations .Values.annotations.pod }}
      annotations:
        {{- with .Values.annotations.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with .Values.components.guac.annotations }}{{- toYaml . | indent 8 }}{{- end }}
      {{- end }}
    spec:
      restartPolicy: {{ .Values.restartPolicy }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets }}
      {{- end }}
      {{- if .Values.applyHardening }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
      {{- if .Values.applyHardening }}
        - name: {{ $values.guac.name }}-tmp-perms
          image: {{ $values.api.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          command:
            - /bin/sh
            - -c
          args:
            - |
              echo "Setting permissions and ownership for /tmp..." && chown -R 1000:1000 /tmp && chmod -R 777 /tmp
              echo "Setting permissions and ownership for /opt/kasm/current..." && chown -R 1000:1000 /opt/kasm/current && chmod -R 0770 /opt/kasm/current
              echo "Done"
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
        {{- end }}
        {{- include "kasm.initContainer" (dict "serviceName" ($values.api.svc) "servicePort" ($values.api.port) "path" "/api/__healthcheck" "schema" "http" "image" ($values.api.image)) | nindent 8 }}
        {{- if .Values.applyHardening }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
      containers:
        - name: {{ $values.guac.name }}-container
          image: {{- $values.guac.image -}}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          env:
            - name: CLUSTER_SIZE
              value: "4"
            - name: REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: service-token
            - name: ZONE
              value: default
            - name: KUBERNETES_SERVICE_HOST
              value: "true"
            - name: SERVER_ADDRESS
              value: {{ $values.proxy.svc }}
            - name: SERVER_PORT
              value: {{ $values.proxy.http }}
          ports:
            - name: {{ $values.guac.name }}-pt
              containerPort: {{ $values.guac.port }}
              protocol: TCP
          {{- if .Values.applyHardening }}
          volumeMounts:
            - name: {{ $values.guac.name }}-data
              mountPath: /tmp
            - name: {{ $values.guac.name }}-readonly
              mountPath: /opt/kasm/current/log
              readOnly: true
            - name: {{ $values.guac.name }}-data
              mountPath: /opt/kasm/current/tmp/guac
          {{- end }}
          {{- if .Values.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" ($values.guac.name)) ) | indent 12 }}
          {{- end }}
          {{- if .Values.addHealthChecks }}
          readinessProbe:
            {{- include "health.http" (dict "path" "/__healthcheck" "portName" (printf "%s-pt" ($values.guac.name)) ) | indent 12 }}
          {{- end }}
          {{- if .Values.components.guac.resources }}
          resources: {{- toYaml .Values.components.guac.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" ($values.guac.component) "size" .Values.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if .Values.applyHardening }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
      {{- if .Values.applyHardening }}
      volumes:
        - name: {{ $values.guac.name }}-data
          emptyDir:
            sizeLimit: 1Gi
        - name: {{ $values.guac.name }}-readonly
          emptyDir:
            sizeLimit: 150Mi
      {{- end }}
{{- end }}
