{{/*
Deploy the RDP Gateway if .Values.components.rdpGateway.enabled set to true
*/}}
{{- $values := (include "kasm.constants" . | fromYaml) -}}
{{- $rdpPort := 3389 -}}
{{- if eq .Values.components.rdpGateway.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $values.rdpGateway.svc }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $values.rdpGateway.svc }}-svc
    app.kubernetes.io/component: {{ $values.rdpGateway.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or $.Values.components.rdpGateway.labels $.Values.extraLabels.service $.Values.labels }}
    {{- with $.Values.components.rdpGateway.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.extraLabels.service }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- with $.Values.annotations.service }}
  annotations: {{ toYaml . | indent 4 }}
  {{- end }}
spec:
  selector:
    app.kubernetes.io/name: {{ $values.rdpGateway.name }}
  ports:
    - name: {{ printf "%s-svc-%s" $values.rdpGateway.svc ($values.rdpGateway.port | toString) }}
      protocol: TCP
      port: {{ $values.rdpGateway.port }}
    - name: {{ printf "%s-svc-%s" $values.rdpGateway.svc ($rdpPort | toString) }}
      protocol: TCP
      port: {{ $rdpPort }}
{{- end }}