{{/*
Deploy the RDP Gateway if .Values.kasmApp.servicesToDeploy.rdpGateway.deploy set to true
*/}}
{{- $rdpGatewayComponent := "rdp-gateway" -}}
{{- $rdpGatewayName := (printf "%s-%s" .Release.Name $rdpGatewayComponent) -}}
{{- $rdpGatewayImage := (printf "%s:%s" .Values.components.rdpGateway.image.repository .Values.components.rdpGateway.image.tag) -}}
{{- $rdpGatewayReplicas := include "replicas.preset" (dict "node" $rdpGatewayComponent "size" .Values.deploymentSize) -}}
{{- if eq .Values.components.rdpGateway.enabled true }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $rdpGatewayName }}-deployment
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $rdpGatewayName }}
    app.kubernetes.io/component: {{ $rdpGatewayComponent }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or $.Values.components.rdpGateway.labels $.Values.extraLabels.deployment $.Value.labels }}
    {{- with $.Values.components.rdpGateway.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.extraLabels.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- if or $.Values.components.rdpGateway.annotations $.Values.annotations.deployment }}
  annotations:
    {{- with $.Values.annotations.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.components.rdpGateway.annotations }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
spec:
  replicas: {{ $rdpGatewayReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 $rdpGatewayReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $rdpGatewayName }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $rdpGatewayName }}
        app.kubernetes.io/component: {{ $rdpGatewayComponent }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
      {{- if or $.Values.components.rdpGateway.labels $.Values.extraLabels.pod $.Value.labels }}
        {{- with $.Values.components.rdpGateway.labels }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.extraLabels.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
      {{- end }}
      {{- if or $.Values.components.rdpGateway.annotations .Values.annotations.pod }}
      annotations:
        {{- with $.Values.annotations.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.components.rdpGateway.annotations }}{{- toYaml . | indent 8 }}{{- end }}
      {{- end }}
    spec:
      restartPolicy: {{ .Values.restartPolicy }}
      {{- if $.Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ $.Values.imagePullSecrets }}
      {{- end }}
      {{- if $.Values.applySecurity }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
        - name: rdp-gateway-tmp-perms
          image: busybox
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -ec
            - |
              echo "Setting ownership and permissions for /usr/local/etc/rdpproxy/var..." && chown -R 1000:1000 /usr/local/etc/rdpproxy/var && chmod -R 777 /usr/local/etc/rdpproxy/var
              echo "Setting ownership and permissions for /opt/kasm/current/log..." && chown -R 1000:1000 /opt/kasm/current/log && chmod -R 777 /opt/kasm/current/log
              echo "Setting ownership and permissions for /tmp" && chown -R 1000:1000 /tmp && chmod -R 777 /tmp
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          volumeMounts:
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /usr/local/etc/rdpproxy/var
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /opt/kasm/current/log
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /tmp
        {{- include "kasm.initContainer" (dict "serviceName" $apiSvcName "servicePort" $apiSvcPort "path" "/api/__healthcheck" "schema" "http") | indent 8 }}
        {{- if $.Values.applySecurity }}
          {{- include "kasm.containerSecurity" . | indent 10 }}
        {{- end }}
      containers:
        - name: {{ $rdpGatewayName }}-container
          image: {{ $rdpGatewayImage }}
          imagePullPolicy: {{ $.Values.imagePullPolicy }}
          env:
            - name: SERVER_ZONE_NAME
              value: "default"
            - name: REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: service-token
            - name: KUBERNETES_SERVICE_HOST
              value: "true"
          ports:
            - name: {{ $rdpGatewayName }}-pt
              containerPort: 5555
              protocol: TCP
          {{- if $.Values.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" $rdpGatewayName) ) | indent 12 }}
          {{- end }}
          {{- if $.Values.addHealthChecks }}
          readinessProbe:
            {{- include "health.http" (dict "path" "/__healthcheck" "portName" (printf "%s-pt" $rdpGatewayName) ) | indent 12 }}
          {{- end }}
          {{- if $.Values.components.rdpGateway.resources }}
          resources: {{- toYaml $.Values.components.rdpGateway.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" "rdp" "size" $.Values.kasmApp.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if $.Values.applySecurity }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
          volumeMounts:
            - name: {{ $proxyComponent }}-cert
              mountPath: /usr/local/etc/rdpproxy/rdpproxy.crt
              subPath: tls.crt
            - name: {{ $proxyComponent }}-cert
              mountPath: /usr/local/etc/rdpproxy/rdpproxy.key
              subPath: tls.key
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /usr/local/etc/rdpproxy/var
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /opt/kasm/current/log
            - name: {{ $rdpGatewayComponent }}-data
              mountPath: /tmp
            - name: {{ $rdpGatewayComponent }}-config
              mountPath: /usr/local/etc/rdpproxy/conf/passthrough.app.config.yaml
              subPath: passthrough.app.config.yaml
      volumes:
        - name: {{ $proxyComponent }}-cert
          secret:
            secretName: {{ $proxyName }}-certs
        - name: {{ $rdpGatewayComponent }}-data
          emptyDir:
            sizeLimit: 1Gi
        - name: {{ $rdpGatewayComponent }}-config
          configMap:
            name: {{ $rdpGatewayName }}-configmap
{{- end }}