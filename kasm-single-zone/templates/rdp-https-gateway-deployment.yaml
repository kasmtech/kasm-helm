{{/*
Deploy the RDP HTTPS Gateway if .Values.kasmApp.servicesToDeploy.rdpHttpsGateway.deploy set to true
*/}}
{{- $values := (include "kasm.constants" . | fromYaml) -}}
{{- $rdpHttpsGatewayReplicas := include "replicas.preset" (dict "node" $values.rdpHttpsGateway.component "size" .Values.deploymentSize) -}}
{{- if eq .Values.components.rdpHttpsGateway.enabled true }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $values.rdpHttpsGateway.name }}-deployment
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $values.rdpHttpsGateway.name }}
    app.kubernetes.io/component: {{ $values.rdpHttpsGateway.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or $.Values.components.rdpHttpsGateway.labels $.Values.extraLabels.deployment $.Values.labels }}
    {{- with $.Values.components.rdpHttpsGateway.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.extraLabels.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- if or $.Values.components.rdpHttpsGateway.annotations .Values.annotations.deployment }}
  annotations:
    {{- with $.Values.annotations.deployment }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.components.rdpHttpsGateway.annotations }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
spec:
  replicas: {{ $rdpHttpsGatewayReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: {{ add1 $rdpHttpsGatewayReplicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ $values.rdpHttpsGateway.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ $values.rdpHttpsGateway.name }}
        app.kubernetes.io/component: {{ $values.rdpHttpsGateway.component }}
        {{- include "kasm.defaultLabels" . | indent 8 }}
      {{- if or $.Values.components.rdpHttpsGateway.labels $.Values.extraLabels.pod $.Values.labels }}
        {{- with $.Values.components.rdpHttpsGateway.labels }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.extraLabels.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
      {{- end }}
      {{- if or $.Values.components.rdpHttpsGateway.annotations $.Values.annotations.pod }}
      annotations:
        {{- with $.Values.annotations.pod }}{{- toYaml . | indent 8 }}{{- end }}
        {{- with $.Values.components.rdpHttpsGateway.annotations }}{{- toYaml . | indent 8 }}{{- end }}
      {{- end }}
    spec:
      restartPolicy: {{ $.Values.restartPolicy }}
      {{- if $.Values.imagePullSecrets }}
      imagePullSecrets:
        - name: {{ $.Values.imagePullSecrets }}
      {{- end }}
      {{- if $.Values.applySecurity }}
        {{- include "kasm.podSecurity" . | indent 6 }}
      {{- end }}
      initContainers:
        {{- include "kasm.initContainer" (dict "serviceName" $values.api.svc "servicePort" $values.api.port "path" "/api/__healthcheck" "schema" "http" "image" $values.api.image) | nindent 8 }}
        {{- if $.Values.applySecurity }}
        - name: {{ $values.rdpHttpsGateway.name }}-tmp-perms
          image: {{ $values.api.image }}
          imagePullPolicy: IfNotPresent
          command:
            - /bin/sh
            - -ec
          args:
            - |
              echo "No customizations needed yet."
          securityContext:
            runAsNonRoot: false
            runAsUser: 0
          volumeMounts:
            - name: {{ $values.rdpHttpsGateway.name }}-data
              mountPath: /opt/kasm/current/log
            - name: {{ $values.rdpHttpsGateway.name }}-data
              mountPath: /tmp
        {{- end }}
      containers:
        - name: {{ $values.rdpHttpsGateway.name }}-container
          image: {{ $values.rdpHttpsGateway.image }}
          imagePullPolicy: {{ $.Values.imagePullPolicy }}
          env:
            - name: SERVER_ZONE_NAME
              value: "default"
            - name: REGISTRATION_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-secrets
                  key: service-token
            - name: KUBERNETES_SERVICE_HOST
              value: "true"
          ports:
            - name: {{ $values.rdpHttpsGateway.name }}-pt
              containerPort: {{ $values.rdpHttpsGateway.port }}
              protocol: TCP
          {{- if $.Values.addHealthChecks }}
          livenessProbe:
            {{- include "health.tcp" (dict "portName" (printf "%s-pt" $values.rdpHttpsGateway.name) ) | indent 12 }}
          {{- end }}
          {{- if $.Values.addHealthChecks }}
          readinessProbe:
            {{- include "health.https" (dict "path" "/rdp-https-gateway/__healthcheck" "portName" (printf "%s-pt" $values.rdpHttpsGateway.name) ) | indent 12 }}
          {{- end }}
          {{- if $.Values.components.rdpHttpsGateway.resources }}
          resources: {{- toYaml $.Values.components.rdpHttpsGateway.resources | nindent 12 }}
          {{- else }}
          resources: {{- include "resources.preset" (dict "node" $values.rdpHttpsGateway.component "size" $.Values.deploymentSize) | nindent 12 }}
          {{- end }}
          {{- if $.Values.applySecurity }}
            {{- include "kasm.containerSecurity" . | indent 10 }}
          {{- end }}
          volumeMounts:
            - name: {{ $values.rdpHttpsGateway.name }}-cert
              mountPath: /opt/rdpgw/server.pem
              subPath: tls.crt
            - name: {{ $values.rdpHttpsGateway.name }}-cert
              mountPath: /opt/rdpgw/key.pem
              subPath: tls.key
            - name: {{ $values.rdpHttpsGateway.name }}-data
              mountPath: /opt/kasm/current/log
            - name: {{ $values.rdpHttpsGateway.name }}-data
              mountPath: /tmp
      volumes:
        - name: {{ $values.rdpHttpsGateway.name }}-cert
          secret:
            secretName: {{ .Values.certificate.secretName }}
        - name: {{ $values.rdpHttpsGateway.name }}-data
          emptyDir:
            sizeLimit: 150Mi
{{- end }}