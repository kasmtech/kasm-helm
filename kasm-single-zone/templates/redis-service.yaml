{{/*
Deploy the Redis if .Values.components.share.enabled set to true
*/}}
{{- $constants := (include "kasm.constants" . | fromYaml) -}}
{{- if eq .Values.components.share.enabled true }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $constants.redis.svc }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $constants.redis.svc }}-svc
    app.kubernetes.io/component: {{ $constants.redis.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or $.Values.components.redis.labels $.Values.extraLabels.service $.Values.labels }}
    {{- with $.Values.components.redis.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.extraLabels.service }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with $.Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- with $.Values.annotations.service }}
  annotations: {{ toYaml . | indent 4 }}
  {{- end }}
spec:
  selector:
    app.kubernetes.io/name: {{ $constants.redis.name }}
  ports:
    - name: {{ printf "%s-svc-%s" $constants.redis.svc ($constants.redis.port | toString) }}
      protocol: TCP
      port: {{ $constants.redis.port }}
{{- end }}