{{/*
Deploy Kasm API Service .Values.database service definition
*/}}
{{- $values := (include "kasm.constants" . | fromYaml) -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $values.db.svc }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $values.db.svc }}-svc
    app.kubernetes.io/component: {{ $values.db.component }}
    {{- include "kasm.defaultLabels" . | indent 4 }}
  {{- if or .Values.database.labels .Values.extraLabels.service .Values.labels }}
    {{- with .Values.database.labels }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.extraLabels.service }}{{- toYaml . | indent 4 }}{{- end }}
    {{- with .Values.labels }}{{- toYaml . | indent 4 }}{{- end }}
  {{- end }}
  {{- with .Values.annotations.service }}
  annotations: {{ toYaml . | indent 4 }}
  {{- end }}
spec:
  selector:
    app.kubernetes.io/name: {{ $values.db.svc }}
  ports:
    - name: {{ printf "%s-svc-%s" $values.db.svc ($values.db.port | toString) }}
      protocol: TCP
      port: {{ $values.db.port }}
